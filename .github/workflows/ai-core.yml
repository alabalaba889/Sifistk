name: Sifistk AI Core tests
on:
  push:
    branches: ["sifistk-10.1.0-ai-core"]
  pull_request:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm test
